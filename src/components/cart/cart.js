import './cart.scss';
import { WORDINGS } from '../../wordings';

export const Cart = () => {
    return (
        <div className="cart">
            <h1>{WORDINGS.CART_COMMING_SOON}</h1>
        </div>
    )
}
